#include "main.h"

/**
 * _abs - Calculates the absolute value
 * @n: This is the number to be calculated
 * Return: The absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
