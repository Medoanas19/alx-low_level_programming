#include "main.h"

/**
 * add - Adding two ints
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: total of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
